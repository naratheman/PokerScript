rude An example of an If-else Statement in PokerScript

 chip x: 0
 excuses x == 0 $.
  cashout hit
 .$ followingexcuses $.
  cashout miss
 .$

 rude An example of a do-while loop in PokerScript. Do => Action, while => contemplating.

action $.
  reveal("What is taking so long!?")
.$
contemplating(condition) 


rude An example of a for-loop in PokerScript. 

```
playingloose(chip x = 0, x lower 10, x increment) {
  playingloose (chip y = 0, y lower 20, y increment) {
    y incrementby 5
  }
}
```